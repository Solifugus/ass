// Package sql implements PROC SQL: parsing and executing a subset of SQL
// (select, where, group by, order by, joins, create table as) over in-memory
// datasets, or bridging to an embedded engine. See sql/DECISION.md.
package sql
