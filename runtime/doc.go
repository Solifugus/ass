// Package runtime executes DATA steps. It models the SAS Program Data Vector
// (PDV) and the implicit row loop, evaluates expressions, and produces output
// datasets in the library (see package table).
package runtime
